[project]
name = "keywordsclub"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.5",
    "pydantic>=2.9.2",
    "python-dotenv>=1.0.1",
    "openai>=1.58.1",
    "uvicorn>=0.32.1",
]

[tool.vercel]
entrypoint = "app.main:app"
